<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BuzzBreakPhone"
        android:forceDarkAllowed="true">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".WelcomeActivity" />
        <activity android:name=".AppUsageActivity" />
        <activity android:name=".BreakActivity" />
        <activity android:name=".BuzzModeActivity" />
        <activity android:name=".SelectAppsActivity" />
        <activity android:name=".TipsActivity" />
        <activity android:name=".DailyReportActivity" />
        <activity android:name=".SettingsActivity" />

        <!-- Background service for usage data collection -->
        <service android:name=".UsageStatsService"
            android:enabled="true"
            android:exported="false" />

        <!-- Receiver for scheduled data updates -->
        <receiver android:name=".DataUpdateReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter android:priority="1000">
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="com.example.buzzbreakphone.UPDATE_USAGE_DATA" />
                <action android:name="com.example.buzzbreakphone.HOURLY_REPORT_RESET" />
            </intent-filter>
        </receiver>

        <receiver android:name=".ReminderReceiver" />
    </application>

</manifest>